name: Aviator Bot

on:
  schedule:
    - cron: "*/5 * * * *"  # Run every 5 minutes (adjust for your needs)
  workflow_dispatch:  # Allow manual trigger from Actions tab

jobs:
  run-bot:
    runs-on: ubuntu-latest  # GitHub's Ubuntu runner

    timeout-minutes: 10  # Timeout the job after 10 minutes

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3  # Checkout your repo

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"  # Node.js version for the bot

      - name: Install dependencies
        run: npm install  # Install the bot's dependencies

      - name: Run the bot
        run: node scraper.js  # Run the bot script
